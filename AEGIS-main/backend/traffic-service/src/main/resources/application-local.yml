spring:
  datasource:
    url: jdbc:h2:file:${user.home}/.smart-emergency-grid/segdb;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
